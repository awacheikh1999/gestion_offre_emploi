{% extends 'base.html.twig' %}

{% block title %}Details de l'utilisateur{% endblock %}

{% block body %}
<style>  
         body{
             background-color:#FFEFD5;
         } 
</style>
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
         <a href="{{ path('app_home')}}" style="color:white" >SEN FORAGE </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>

      </ul>
      <form class="d-flex">
        <input class="form-control me-sm-2" type="text" placeholder="Search">
        <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
      </form>
    </div>
  </div>
</nav>
<div class="container">
 </br> </br> </br>
    <h1>Utilisateur № {{ user.id }}</h1>

    <table class="table">
        <tbody>
            
            <tr>
                <th>Username</th>
                <td>{{ user.username }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>******</td>
            </tr>
        </tbody>
    </table>

                <a href="{{ path('app_user_index') }}" class="btn btn-secondary"> Liste des utilisateurs</a>

    <a href="{{ path('app_user_edit', {'id': user.id}) }}"class="btn btn-warning" >Modifier</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
